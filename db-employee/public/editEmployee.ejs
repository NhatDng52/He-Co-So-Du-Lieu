<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sửa thông tin nhân viên</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
        }

        .container {
            max-width: 600px;
            margin: 50px auto;
            padding: 20px;
            background-color: #ffffff;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        select {
            padding: 10px;
            margin-top: 5px;
            font-size: 16px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        h1 {
            color: #333;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        label {
            margin-top: 10px;
            margin-bottom: 5px;
            font-weight: bold;
        }

        input[type="text"],
        input[type="number"] {
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 3px;
            margin-bottom: 15px;
        }

        button {
            padding: 10px;
            background-color: #007B8F;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
        }

        button:hover {
            background-color: #005F6B;
        }

        a {
            display: block;
            margin-top: 15px;
            text-decoration: none;
            color: #007B8F;
        }

        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Sửa thông tin nhân viên</h1>

        <form action="/employees/update/<%= employee.id %>" method="POST">
            <label for="name">Tên</label>
            <input type="text" id="name" name="name" value = "<%= employee.empname %>" required >

            <label for="sex">Giới tính</label>
            <select id="sex" name="sex">
                <option value="M"<%= employee.sex === 'M' ? 'selected' : '' %>>Nam</option>
                <option value="F"<%= employee.sex === 'F' ? 'selected' : '' %>>Nữ</option>
                <option value="N"<%= employee.sex === 'N' ? 'selected' : '' %>>Khác</option>
            </select>

            <label for="age">Tuổi</label>
            <input type="number" id="age" name="age" min="18" max="120" value="<%= employee.age %>" required>

            <label for="address">Địa chỉ</label>
            <input type="text" id="address" name="address" value = "<%= employee.address %>" required>

            <label for="supervisor">Người quản lý:</label>
            <select id="supervisor" name="supervisor">
                <option value=""></option>
                <% supervisors.forEach(supervisor => { %>
                    <option value="<%= supervisor.id %>" <%= supervisor.id == employee.sid ? 'selected' : '' %>>
                        <%= supervisor.fname %>
                    </option>
                <% }) %>
            </select>

            <label for="CCCD">CCCD</label>
            <input type="text" id="CCCD" name="CCCD" value = "<%= employee.cccd %>">

            <label for="email">Email</label>
            <input type="text" id="email" name="email" value = "<%= employee.email %>">

            <label for="jobType">Loại hình công việc</label>
            <select id="jobType" name="jobType">
                <option value="FULL TIME"<%= employee.job_type === 'FULL TIME' ? 'selected' : '' %>>Toàn thời gian</option>
                <option value="PART TIME"<%= employee.job_type === 'PART TIME'? 'selected' : '' %>>Bán thời gian</option>
            </select>
            <label for="salary">Lương</label>
            <input type="text" id="salary" name="salary" value = "<%= employee.salary %>">

            <button type="submit">Lưu thông tin</button>
        </form>
        <a href="/employees">Quay lại danh sách nhân viên</a>
    </div>
</body>
</html>
